/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import { VOBLE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SUBMIT_GUESS_DISCRIMINATOR = new Uint8Array([
  61, 124, 32, 227, 64, 198, 252, 3,
]);

export function getSubmitGuessDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SUBMIT_GUESS_DISCRIMINATOR,
  );
}

export type SubmitGuessInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountSession extends string | AccountMeta<string> = string,
  TAccountTargetWord extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSession extends string
        ? WritableAccount<TAccountSession>
        : TAccountSession,
      TAccountTargetWord extends string
        ? ReadonlyAccount<TAccountTargetWord>
        : TAccountTargetWord,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SubmitGuessInstructionData = {
  discriminator: ReadonlyUint8Array;
  guess: string;
};

export type SubmitGuessInstructionDataArgs = { guess: string };

export function getSubmitGuessInstructionDataEncoder(): Encoder<SubmitGuessInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["guess", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: SUBMIT_GUESS_DISCRIMINATOR }),
  );
}

export function getSubmitGuessInstructionDataDecoder(): Decoder<SubmitGuessInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["guess", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getSubmitGuessInstructionDataCodec(): Codec<
  SubmitGuessInstructionDataArgs,
  SubmitGuessInstructionData
> {
  return combineCodec(
    getSubmitGuessInstructionDataEncoder(),
    getSubmitGuessInstructionDataDecoder(),
  );
}

export type SubmitGuessAsyncInput<
  TAccountSession extends string = string,
  TAccountTargetWord extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  session: Address<TAccountSession>;
  /** The target word account (private, but readable by TEE for evaluation) */
  targetWord: Address<TAccountTargetWord>;
  eventAuthority?: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  guess: SubmitGuessInstructionDataArgs["guess"];
};

export async function getSubmitGuessInstructionAsync<
  TAccountSession extends string,
  TAccountTargetWord extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof VOBLE_PROGRAM_ADDRESS,
>(
  input: SubmitGuessAsyncInput<
    TAccountSession,
    TAccountTargetWord,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SubmitGuessInstruction<
    TProgramAddress,
    TAccountSession,
    TAccountTargetWord,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOBLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    session: { value: input.session ?? null, isWritable: true },
    targetWord: { value: input.targetWord ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.eventAuthority.value) {
    accounts.eventAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            95, 95, 101, 118, 101, 110, 116, 95, 97, 117, 116, 104, 111, 114,
            105, 116, 121,
          ]),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.targetWord),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.program),
    ],
    data: getSubmitGuessInstructionDataEncoder().encode(
      args as SubmitGuessInstructionDataArgs,
    ),
    programAddress,
  } as SubmitGuessInstruction<
    TProgramAddress,
    TAccountSession,
    TAccountTargetWord,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type SubmitGuessInput<
  TAccountSession extends string = string,
  TAccountTargetWord extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  session: Address<TAccountSession>;
  /** The target word account (private, but readable by TEE for evaluation) */
  targetWord: Address<TAccountTargetWord>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  guess: SubmitGuessInstructionDataArgs["guess"];
};

export function getSubmitGuessInstruction<
  TAccountSession extends string,
  TAccountTargetWord extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof VOBLE_PROGRAM_ADDRESS,
>(
  input: SubmitGuessInput<
    TAccountSession,
    TAccountTargetWord,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SubmitGuessInstruction<
  TProgramAddress,
  TAccountSession,
  TAccountTargetWord,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOBLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    session: { value: input.session ?? null, isWritable: true },
    targetWord: { value: input.targetWord ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.targetWord),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.program),
    ],
    data: getSubmitGuessInstructionDataEncoder().encode(
      args as SubmitGuessInstructionDataArgs,
    ),
    programAddress,
  } as SubmitGuessInstruction<
    TProgramAddress,
    TAccountSession,
    TAccountTargetWord,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedSubmitGuessInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    session: TAccountMetas[0];
    /** The target word account (private, but readable by TEE for evaluation) */
    targetWord: TAccountMetas[1];
    eventAuthority: TAccountMetas[2];
    program: TAccountMetas[3];
  };
  data: SubmitGuessInstructionData;
};

export function parseSubmitGuessInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSubmitGuessInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      session: getNextAccount(),
      targetWord: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getSubmitGuessInstructionDataDecoder().decode(instruction.data),
  };
}
