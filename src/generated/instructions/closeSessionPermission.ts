/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VOBLE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CLOSE_SESSION_PERMISSION_DISCRIMINATOR = new Uint8Array([
  97, 3, 26, 72, 81, 38, 231, 188,
]);

export function getCloseSessionPermissionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_SESSION_PERMISSION_DISCRIMINATOR,
  );
}

export type CloseSessionPermissionInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountSession extends string | AccountMeta<string> = string,
  TAccountPermission extends string | AccountMeta<string> = string,
  TAccountPermissionProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountPlayer extends string
        ? ReadonlySignerAccount<TAccountPlayer> &
            AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountSession extends string
        ? ReadonlyAccount<TAccountSession>
        : TAccountSession,
      TAccountPermission extends string
        ? WritableAccount<TAccountPermission>
        : TAccountPermission,
      TAccountPermissionProgram extends string
        ? ReadonlyAccount<TAccountPermissionProgram>
        : TAccountPermissionProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseSessionPermissionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseSessionPermissionInstructionDataArgs = {};

export function getCloseSessionPermissionInstructionDataEncoder(): FixedSizeEncoder<CloseSessionPermissionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CLOSE_SESSION_PERMISSION_DISCRIMINATOR,
    }),
  );
}

export function getCloseSessionPermissionInstructionDataDecoder(): FixedSizeDecoder<CloseSessionPermissionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseSessionPermissionInstructionDataCodec(): FixedSizeCodec<
  CloseSessionPermissionInstructionDataArgs,
  CloseSessionPermissionInstructionData
> {
  return combineCodec(
    getCloseSessionPermissionInstructionDataEncoder(),
    getCloseSessionPermissionInstructionDataDecoder(),
  );
}

export type CloseSessionPermissionAsyncInput<
  TAccountPayer extends string = string,
  TAccountPlayer extends string = string,
  TAccountSession extends string = string,
  TAccountPermission extends string = string,
  TAccountPermissionProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** The player who owns the session (authority) */
  player: TransactionSigner<TAccountPlayer>;
  /** The session account */
  session?: Address<TAccountSession>;
  permission: Address<TAccountPermission>;
  permissionProgram: Address<TAccountPermissionProgram>;
};

export async function getCloseSessionPermissionInstructionAsync<
  TAccountPayer extends string,
  TAccountPlayer extends string,
  TAccountSession extends string,
  TAccountPermission extends string,
  TAccountPermissionProgram extends string,
  TProgramAddress extends Address = typeof VOBLE_PROGRAM_ADDRESS,
>(
  input: CloseSessionPermissionAsyncInput<
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountPermission,
    TAccountPermissionProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseSessionPermissionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountPermission,
    TAccountPermissionProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOBLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: false },
    permission: { value: input.permission ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.session.value) {
    accounts.session.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([115, 101, 115, 115, 105, 111, 110]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.player.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.permission),
      getAccountMeta(accounts.permissionProgram),
    ],
    data: getCloseSessionPermissionInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseSessionPermissionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountPermission,
    TAccountPermissionProgram
  >);
}

export type CloseSessionPermissionInput<
  TAccountPayer extends string = string,
  TAccountPlayer extends string = string,
  TAccountSession extends string = string,
  TAccountPermission extends string = string,
  TAccountPermissionProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** The player who owns the session (authority) */
  player: TransactionSigner<TAccountPlayer>;
  /** The session account */
  session: Address<TAccountSession>;
  permission: Address<TAccountPermission>;
  permissionProgram: Address<TAccountPermissionProgram>;
};

export function getCloseSessionPermissionInstruction<
  TAccountPayer extends string,
  TAccountPlayer extends string,
  TAccountSession extends string,
  TAccountPermission extends string,
  TAccountPermissionProgram extends string,
  TProgramAddress extends Address = typeof VOBLE_PROGRAM_ADDRESS,
>(
  input: CloseSessionPermissionInput<
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountPermission,
    TAccountPermissionProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CloseSessionPermissionInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountPlayer,
  TAccountSession,
  TAccountPermission,
  TAccountPermissionProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOBLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: false },
    permission: { value: input.permission ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.permission),
      getAccountMeta(accounts.permissionProgram),
    ],
    data: getCloseSessionPermissionInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseSessionPermissionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountPermission,
    TAccountPermissionProgram
  >);
}

export type ParsedCloseSessionPermissionInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    /** The player who owns the session (authority) */
    player: TAccountMetas[1];
    /** The session account */
    session: TAccountMetas[2];
    permission: TAccountMetas[3];
    permissionProgram: TAccountMetas[4];
  };
  data: CloseSessionPermissionInstructionData;
};

export function parseCloseSessionPermissionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseSessionPermissionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      player: getNextAccount(),
      session: getNextAccount(),
      permission: getNextAccount(),
      permissionProgram: getNextAccount(),
    },
    data: getCloseSessionPermissionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
