/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VOBLE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CLOSE_TARGET_WORD_PERMISSION_DISCRIMINATOR = new Uint8Array([
  5, 165, 34, 213, 21, 9, 51, 26,
]);

export function getCloseTargetWordPermissionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_TARGET_WORD_PERMISSION_DISCRIMINATOR,
  );
}

export type CloseTargetWordPermissionInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountTargetWord extends string | AccountMeta<string> = string,
  TAccountPermission extends string | AccountMeta<string> = string,
  TAccountPermissionProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountPlayer extends string
        ? ReadonlySignerAccount<TAccountPlayer> &
            AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountTargetWord extends string
        ? ReadonlyAccount<TAccountTargetWord>
        : TAccountTargetWord,
      TAccountPermission extends string
        ? WritableAccount<TAccountPermission>
        : TAccountPermission,
      TAccountPermissionProgram extends string
        ? ReadonlyAccount<TAccountPermissionProgram>
        : TAccountPermissionProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseTargetWordPermissionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseTargetWordPermissionInstructionDataArgs = {};

export function getCloseTargetWordPermissionInstructionDataEncoder(): FixedSizeEncoder<CloseTargetWordPermissionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CLOSE_TARGET_WORD_PERMISSION_DISCRIMINATOR,
    }),
  );
}

export function getCloseTargetWordPermissionInstructionDataDecoder(): FixedSizeDecoder<CloseTargetWordPermissionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseTargetWordPermissionInstructionDataCodec(): FixedSizeCodec<
  CloseTargetWordPermissionInstructionDataArgs,
  CloseTargetWordPermissionInstructionData
> {
  return combineCodec(
    getCloseTargetWordPermissionInstructionDataEncoder(),
    getCloseTargetWordPermissionInstructionDataDecoder(),
  );
}

export type CloseTargetWordPermissionAsyncInput<
  TAccountPayer extends string = string,
  TAccountPlayer extends string = string,
  TAccountTargetWord extends string = string,
  TAccountPermission extends string = string,
  TAccountPermissionProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** The player who owns the target word (authority) */
  player: TransactionSigner<TAccountPlayer>;
  /** The target word account */
  targetWord?: Address<TAccountTargetWord>;
  permission: Address<TAccountPermission>;
  permissionProgram: Address<TAccountPermissionProgram>;
};

export async function getCloseTargetWordPermissionInstructionAsync<
  TAccountPayer extends string,
  TAccountPlayer extends string,
  TAccountTargetWord extends string,
  TAccountPermission extends string,
  TAccountPermissionProgram extends string,
  TProgramAddress extends Address = typeof VOBLE_PROGRAM_ADDRESS,
>(
  input: CloseTargetWordPermissionAsyncInput<
    TAccountPayer,
    TAccountPlayer,
    TAccountTargetWord,
    TAccountPermission,
    TAccountPermissionProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseTargetWordPermissionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountTargetWord,
    TAccountPermission,
    TAccountPermissionProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOBLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: false },
    targetWord: { value: input.targetWord ?? null, isWritable: false },
    permission: { value: input.permission ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.targetWord.value) {
    accounts.targetWord.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 97, 114, 103, 101, 116, 95, 119, 111, 114, 100]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.player.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.targetWord),
      getAccountMeta(accounts.permission),
      getAccountMeta(accounts.permissionProgram),
    ],
    data: getCloseTargetWordPermissionInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseTargetWordPermissionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountTargetWord,
    TAccountPermission,
    TAccountPermissionProgram
  >);
}

export type CloseTargetWordPermissionInput<
  TAccountPayer extends string = string,
  TAccountPlayer extends string = string,
  TAccountTargetWord extends string = string,
  TAccountPermission extends string = string,
  TAccountPermissionProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** The player who owns the target word (authority) */
  player: TransactionSigner<TAccountPlayer>;
  /** The target word account */
  targetWord: Address<TAccountTargetWord>;
  permission: Address<TAccountPermission>;
  permissionProgram: Address<TAccountPermissionProgram>;
};

export function getCloseTargetWordPermissionInstruction<
  TAccountPayer extends string,
  TAccountPlayer extends string,
  TAccountTargetWord extends string,
  TAccountPermission extends string,
  TAccountPermissionProgram extends string,
  TProgramAddress extends Address = typeof VOBLE_PROGRAM_ADDRESS,
>(
  input: CloseTargetWordPermissionInput<
    TAccountPayer,
    TAccountPlayer,
    TAccountTargetWord,
    TAccountPermission,
    TAccountPermissionProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CloseTargetWordPermissionInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountPlayer,
  TAccountTargetWord,
  TAccountPermission,
  TAccountPermissionProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOBLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: false },
    targetWord: { value: input.targetWord ?? null, isWritable: false },
    permission: { value: input.permission ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.targetWord),
      getAccountMeta(accounts.permission),
      getAccountMeta(accounts.permissionProgram),
    ],
    data: getCloseTargetWordPermissionInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseTargetWordPermissionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountTargetWord,
    TAccountPermission,
    TAccountPermissionProgram
  >);
}

export type ParsedCloseTargetWordPermissionInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    /** The player who owns the target word (authority) */
    player: TAccountMetas[1];
    /** The target word account */
    targetWord: TAccountMetas[2];
    permission: TAccountMetas[3];
    permissionProgram: TAccountMetas[4];
  };
  data: CloseTargetWordPermissionInstructionData;
};

export function parseCloseTargetWordPermissionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseTargetWordPermissionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      player: getNextAccount(),
      targetWord: getNextAccount(),
      permission: getNextAccount(),
      permissionProgram: getNextAccount(),
    },
    data: getCloseTargetWordPermissionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
