/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VOBLE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const RESET_SESSION_DISCRIMINATOR = new Uint8Array([
  207, 171, 19, 218, 22, 160, 71, 53,
]);

export function getResetSessionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RESET_SESSION_DISCRIMINATOR,
  );
}

export type ResetSessionInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSession extends string | AccountMeta<string> = string,
  TAccountTargetWord extends string | AccountMeta<string> = string,
  TAccountUserProfile extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSession extends string
        ? WritableAccount<TAccountSession>
        : TAccountSession,
      TAccountTargetWord extends string
        ? WritableAccount<TAccountTargetWord>
        : TAccountTargetWord,
      TAccountUserProfile extends string
        ? ReadonlyAccount<TAccountUserProfile>
        : TAccountUserProfile,
      ...TRemainingAccounts,
    ]
  >;

export type ResetSessionInstructionData = {
  discriminator: ReadonlyUint8Array;
  periodId: string;
};

export type ResetSessionInstructionDataArgs = { periodId: string };

export function getResetSessionInstructionDataEncoder(): Encoder<ResetSessionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["periodId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: RESET_SESSION_DISCRIMINATOR }),
  );
}

export function getResetSessionInstructionDataDecoder(): Decoder<ResetSessionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["periodId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getResetSessionInstructionDataCodec(): Codec<
  ResetSessionInstructionDataArgs,
  ResetSessionInstructionData
> {
  return combineCodec(
    getResetSessionInstructionDataEncoder(),
    getResetSessionInstructionDataDecoder(),
  );
}

export type ResetSessionInput<
  TAccountPayer extends string = string,
  TAccountSession extends string = string,
  TAccountTargetWord extends string = string,
  TAccountUserProfile extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  session: Address<TAccountSession>;
  /** The target word account for storing the secret word */
  targetWord: Address<TAccountTargetWord>;
  userProfile: Address<TAccountUserProfile>;
  periodId: ResetSessionInstructionDataArgs["periodId"];
};

export function getResetSessionInstruction<
  TAccountPayer extends string,
  TAccountSession extends string,
  TAccountTargetWord extends string,
  TAccountUserProfile extends string,
  TProgramAddress extends Address = typeof VOBLE_PROGRAM_ADDRESS,
>(
  input: ResetSessionInput<
    TAccountPayer,
    TAccountSession,
    TAccountTargetWord,
    TAccountUserProfile
  >,
  config?: { programAddress?: TProgramAddress },
): ResetSessionInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountSession,
  TAccountTargetWord,
  TAccountUserProfile
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOBLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    session: { value: input.session ?? null, isWritable: true },
    targetWord: { value: input.targetWord ?? null, isWritable: true },
    userProfile: { value: input.userProfile ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.targetWord),
      getAccountMeta(accounts.userProfile),
    ],
    data: getResetSessionInstructionDataEncoder().encode(
      args as ResetSessionInstructionDataArgs,
    ),
    programAddress,
  } as ResetSessionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountSession,
    TAccountTargetWord,
    TAccountUserProfile
  >);
}

export type ParsedResetSessionInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    session: TAccountMetas[1];
    /** The target word account for storing the secret word */
    targetWord: TAccountMetas[2];
    userProfile: TAccountMetas[3];
  };
  data: ResetSessionInstructionData;
};

export function parseResetSessionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedResetSessionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      session: getNextAccount(),
      targetWord: getNextAccount(),
      userProfile: getNextAccount(),
    },
    data: getResetSessionInstructionDataDecoder().decode(instruction.data),
  };
}
