/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VOBLE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const UNDELEGATE_SESSION_PERMISSION_DISCRIMINATOR = new Uint8Array([
  146, 165, 95, 13, 27, 74, 161, 250,
]);

export function getUndelegateSessionPermissionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UNDELEGATE_SESSION_PERMISSION_DISCRIMINATOR,
  );
}

export type UndelegateSessionPermissionInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountSession extends string | AccountMeta<string> = string,
  TAccountPermission extends string | AccountMeta<string> = string,
  TAccountPermissionProgram extends string | AccountMeta<string> = string,
  TAccountMagicProgram extends string | AccountMeta<string> = string,
  TAccountMagicContext extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountPlayer extends string
        ? ReadonlyAccount<TAccountPlayer>
        : TAccountPlayer,
      TAccountSession extends string
        ? WritableAccount<TAccountSession>
        : TAccountSession,
      TAccountPermission extends string
        ? WritableAccount<TAccountPermission>
        : TAccountPermission,
      TAccountPermissionProgram extends string
        ? ReadonlyAccount<TAccountPermissionProgram>
        : TAccountPermissionProgram,
      TAccountMagicProgram extends string
        ? ReadonlyAccount<TAccountMagicProgram>
        : TAccountMagicProgram,
      TAccountMagicContext extends string
        ? WritableAccount<TAccountMagicContext>
        : TAccountMagicContext,
      ...TRemainingAccounts,
    ]
  >;

export type UndelegateSessionPermissionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type UndelegateSessionPermissionInstructionDataArgs = {};

export function getUndelegateSessionPermissionInstructionDataEncoder(): FixedSizeEncoder<UndelegateSessionPermissionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: UNDELEGATE_SESSION_PERMISSION_DISCRIMINATOR,
    }),
  );
}

export function getUndelegateSessionPermissionInstructionDataDecoder(): FixedSizeDecoder<UndelegateSessionPermissionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getUndelegateSessionPermissionInstructionDataCodec(): FixedSizeCodec<
  UndelegateSessionPermissionInstructionDataArgs,
  UndelegateSessionPermissionInstructionData
> {
  return combineCodec(
    getUndelegateSessionPermissionInstructionDataEncoder(),
    getUndelegateSessionPermissionInstructionDataDecoder(),
  );
}

export type UndelegateSessionPermissionAsyncInput<
  TAccountPayer extends string = string,
  TAccountPlayer extends string = string,
  TAccountSession extends string = string,
  TAccountPermission extends string = string,
  TAccountPermissionProgram extends string = string,
  TAccountMagicProgram extends string = string,
  TAccountMagicContext extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  player: Address<TAccountPlayer>;
  session?: Address<TAccountSession>;
  permission: Address<TAccountPermission>;
  permissionProgram: Address<TAccountPermissionProgram>;
  magicProgram: Address<TAccountMagicProgram>;
  magicContext: Address<TAccountMagicContext>;
};

export async function getUndelegateSessionPermissionInstructionAsync<
  TAccountPayer extends string,
  TAccountPlayer extends string,
  TAccountSession extends string,
  TAccountPermission extends string,
  TAccountPermissionProgram extends string,
  TAccountMagicProgram extends string,
  TAccountMagicContext extends string,
  TProgramAddress extends Address = typeof VOBLE_PROGRAM_ADDRESS,
>(
  input: UndelegateSessionPermissionAsyncInput<
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountPermission,
    TAccountPermissionProgram,
    TAccountMagicProgram,
    TAccountMagicContext
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UndelegateSessionPermissionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountPermission,
    TAccountPermissionProgram,
    TAccountMagicProgram,
    TAccountMagicContext
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOBLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: true },
    permission: { value: input.permission ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
    magicContext: { value: input.magicContext ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.session.value) {
    accounts.session.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([115, 101, 115, 115, 105, 111, 110]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.player.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.permission),
      getAccountMeta(accounts.permissionProgram),
      getAccountMeta(accounts.magicProgram),
      getAccountMeta(accounts.magicContext),
    ],
    data: getUndelegateSessionPermissionInstructionDataEncoder().encode({}),
    programAddress,
  } as UndelegateSessionPermissionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountPermission,
    TAccountPermissionProgram,
    TAccountMagicProgram,
    TAccountMagicContext
  >);
}

export type UndelegateSessionPermissionInput<
  TAccountPayer extends string = string,
  TAccountPlayer extends string = string,
  TAccountSession extends string = string,
  TAccountPermission extends string = string,
  TAccountPermissionProgram extends string = string,
  TAccountMagicProgram extends string = string,
  TAccountMagicContext extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  player: Address<TAccountPlayer>;
  session: Address<TAccountSession>;
  permission: Address<TAccountPermission>;
  permissionProgram: Address<TAccountPermissionProgram>;
  magicProgram: Address<TAccountMagicProgram>;
  magicContext: Address<TAccountMagicContext>;
};

export function getUndelegateSessionPermissionInstruction<
  TAccountPayer extends string,
  TAccountPlayer extends string,
  TAccountSession extends string,
  TAccountPermission extends string,
  TAccountPermissionProgram extends string,
  TAccountMagicProgram extends string,
  TAccountMagicContext extends string,
  TProgramAddress extends Address = typeof VOBLE_PROGRAM_ADDRESS,
>(
  input: UndelegateSessionPermissionInput<
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountPermission,
    TAccountPermissionProgram,
    TAccountMagicProgram,
    TAccountMagicContext
  >,
  config?: { programAddress?: TProgramAddress },
): UndelegateSessionPermissionInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountPlayer,
  TAccountSession,
  TAccountPermission,
  TAccountPermissionProgram,
  TAccountMagicProgram,
  TAccountMagicContext
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOBLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: true },
    permission: { value: input.permission ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
    magicContext: { value: input.magicContext ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.permission),
      getAccountMeta(accounts.permissionProgram),
      getAccountMeta(accounts.magicProgram),
      getAccountMeta(accounts.magicContext),
    ],
    data: getUndelegateSessionPermissionInstructionDataEncoder().encode({}),
    programAddress,
  } as UndelegateSessionPermissionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountPermission,
    TAccountPermissionProgram,
    TAccountMagicProgram,
    TAccountMagicContext
  >);
}

export type ParsedUndelegateSessionPermissionInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    player: TAccountMetas[1];
    session: TAccountMetas[2];
    permission: TAccountMetas[3];
    permissionProgram: TAccountMetas[4];
    magicProgram: TAccountMetas[5];
    magicContext: TAccountMetas[6];
  };
  data: UndelegateSessionPermissionInstructionData;
};

export function parseUndelegateSessionPermissionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUndelegateSessionPermissionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      player: getNextAccount(),
      session: getNextAccount(),
      permission: getNextAccount(),
      permissionProgram: getNextAccount(),
      magicProgram: getNextAccount(),
      magicContext: getNextAccount(),
    },
    data: getUndelegateSessionPermissionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
