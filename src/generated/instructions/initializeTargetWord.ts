/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VOBLE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const INITIALIZE_TARGET_WORD_DISCRIMINATOR = new Uint8Array([
  144, 103, 191, 73, 184, 149, 242, 84,
]);

export function getInitializeTargetWordDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_TARGET_WORD_DISCRIMINATOR,
  );
}

export type InitializeTargetWordInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountSession extends string | AccountMeta<string> = string,
  TAccountTargetWord extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountPlayer extends string
        ? ReadonlySignerAccount<TAccountPlayer> &
            AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountSession extends string
        ? ReadonlyAccount<TAccountSession>
        : TAccountSession,
      TAccountTargetWord extends string
        ? WritableAccount<TAccountTargetWord>
        : TAccountTargetWord,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeTargetWordInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeTargetWordInstructionDataArgs = {};

export function getInitializeTargetWordInstructionDataEncoder(): FixedSizeEncoder<InitializeTargetWordInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_TARGET_WORD_DISCRIMINATOR,
    }),
  );
}

export function getInitializeTargetWordInstructionDataDecoder(): FixedSizeDecoder<InitializeTargetWordInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeTargetWordInstructionDataCodec(): FixedSizeCodec<
  InitializeTargetWordInstructionDataArgs,
  InitializeTargetWordInstructionData
> {
  return combineCodec(
    getInitializeTargetWordInstructionDataEncoder(),
    getInitializeTargetWordInstructionDataDecoder(),
  );
}

export type InitializeTargetWordAsyncInput<
  TAccountPayer extends string = string,
  TAccountPlayer extends string = string,
  TAccountSession extends string = string,
  TAccountTargetWord extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** The player who owns the session */
  player: TransactionSigner<TAccountPlayer>;
  /** The session account */
  session?: Address<TAccountSession>;
  /** The target word account to create */
  targetWord?: Address<TAccountTargetWord>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializeTargetWordInstructionAsync<
  TAccountPayer extends string,
  TAccountPlayer extends string,
  TAccountSession extends string,
  TAccountTargetWord extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VOBLE_PROGRAM_ADDRESS,
>(
  input: InitializeTargetWordAsyncInput<
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountTargetWord,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeTargetWordInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountTargetWord,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOBLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: false },
    targetWord: { value: input.targetWord ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.session.value) {
    accounts.session.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([115, 101, 115, 115, 105, 111, 110]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.player.value)),
      ],
    });
  }
  if (!accounts.targetWord.value) {
    accounts.targetWord.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 97, 114, 103, 101, 116, 95, 119, 111, 114, 100]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.player.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.targetWord),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeTargetWordInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeTargetWordInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountTargetWord,
    TAccountSystemProgram
  >);
}

export type InitializeTargetWordInput<
  TAccountPayer extends string = string,
  TAccountPlayer extends string = string,
  TAccountSession extends string = string,
  TAccountTargetWord extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** The player who owns the session */
  player: TransactionSigner<TAccountPlayer>;
  /** The session account */
  session: Address<TAccountSession>;
  /** The target word account to create */
  targetWord: Address<TAccountTargetWord>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeTargetWordInstruction<
  TAccountPayer extends string,
  TAccountPlayer extends string,
  TAccountSession extends string,
  TAccountTargetWord extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VOBLE_PROGRAM_ADDRESS,
>(
  input: InitializeTargetWordInput<
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountTargetWord,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeTargetWordInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountPlayer,
  TAccountSession,
  TAccountTargetWord,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOBLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: false },
    targetWord: { value: input.targetWord ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.targetWord),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeTargetWordInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeTargetWordInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountTargetWord,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeTargetWordInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    /** The player who owns the session */
    player: TAccountMetas[1];
    /** The session account */
    session: TAccountMetas[2];
    /** The target word account to create */
    targetWord: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: InitializeTargetWordInstructionData;
};

export function parseInitializeTargetWordInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeTargetWordInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      player: getNextAccount(),
      session: getNextAccount(),
      targetWord: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeTargetWordInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
