/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VOBLE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CREATE_TARGET_WORD_PERMISSION_DISCRIMINATOR = new Uint8Array([
  232, 15, 148, 230, 46, 109, 210, 116,
]);

export function getCreateTargetWordPermissionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_TARGET_WORD_PERMISSION_DISCRIMINATOR,
  );
}

export type CreateTargetWordPermissionInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountTargetWord extends string | AccountMeta<string> = string,
  TAccountPermission extends string | AccountMeta<string> = string,
  TAccountPermissionProgram extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountPlayer extends string
        ? ReadonlySignerAccount<TAccountPlayer> &
            AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountTargetWord extends string
        ? ReadonlyAccount<TAccountTargetWord>
        : TAccountTargetWord,
      TAccountPermission extends string
        ? WritableAccount<TAccountPermission>
        : TAccountPermission,
      TAccountPermissionProgram extends string
        ? ReadonlyAccount<TAccountPermissionProgram>
        : TAccountPermissionProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateTargetWordPermissionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CreateTargetWordPermissionInstructionDataArgs = {};

export function getCreateTargetWordPermissionInstructionDataEncoder(): FixedSizeEncoder<CreateTargetWordPermissionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CREATE_TARGET_WORD_PERMISSION_DISCRIMINATOR,
    }),
  );
}

export function getCreateTargetWordPermissionInstructionDataDecoder(): FixedSizeDecoder<CreateTargetWordPermissionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCreateTargetWordPermissionInstructionDataCodec(): FixedSizeCodec<
  CreateTargetWordPermissionInstructionDataArgs,
  CreateTargetWordPermissionInstructionData
> {
  return combineCodec(
    getCreateTargetWordPermissionInstructionDataEncoder(),
    getCreateTargetWordPermissionInstructionDataDecoder(),
  );
}

export type CreateTargetWordPermissionAsyncInput<
  TAccountPayer extends string = string,
  TAccountPlayer extends string = string,
  TAccountTargetWord extends string = string,
  TAccountPermission extends string = string,
  TAccountPermissionProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** The player who owns the target word */
  player: TransactionSigner<TAccountPlayer>;
  /** The target word account to protect */
  targetWord?: Address<TAccountTargetWord>;
  permission: Address<TAccountPermission>;
  permissionProgram: Address<TAccountPermissionProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getCreateTargetWordPermissionInstructionAsync<
  TAccountPayer extends string,
  TAccountPlayer extends string,
  TAccountTargetWord extends string,
  TAccountPermission extends string,
  TAccountPermissionProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VOBLE_PROGRAM_ADDRESS,
>(
  input: CreateTargetWordPermissionAsyncInput<
    TAccountPayer,
    TAccountPlayer,
    TAccountTargetWord,
    TAccountPermission,
    TAccountPermissionProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateTargetWordPermissionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountTargetWord,
    TAccountPermission,
    TAccountPermissionProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOBLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: false },
    targetWord: { value: input.targetWord ?? null, isWritable: false },
    permission: { value: input.permission ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.targetWord.value) {
    accounts.targetWord.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 97, 114, 103, 101, 116, 95, 119, 111, 114, 100]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.player.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.targetWord),
      getAccountMeta(accounts.permission),
      getAccountMeta(accounts.permissionProgram),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateTargetWordPermissionInstructionDataEncoder().encode({}),
    programAddress,
  } as CreateTargetWordPermissionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountTargetWord,
    TAccountPermission,
    TAccountPermissionProgram,
    TAccountSystemProgram
  >);
}

export type CreateTargetWordPermissionInput<
  TAccountPayer extends string = string,
  TAccountPlayer extends string = string,
  TAccountTargetWord extends string = string,
  TAccountPermission extends string = string,
  TAccountPermissionProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** The player who owns the target word */
  player: TransactionSigner<TAccountPlayer>;
  /** The target word account to protect */
  targetWord: Address<TAccountTargetWord>;
  permission: Address<TAccountPermission>;
  permissionProgram: Address<TAccountPermissionProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getCreateTargetWordPermissionInstruction<
  TAccountPayer extends string,
  TAccountPlayer extends string,
  TAccountTargetWord extends string,
  TAccountPermission extends string,
  TAccountPermissionProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VOBLE_PROGRAM_ADDRESS,
>(
  input: CreateTargetWordPermissionInput<
    TAccountPayer,
    TAccountPlayer,
    TAccountTargetWord,
    TAccountPermission,
    TAccountPermissionProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateTargetWordPermissionInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountPlayer,
  TAccountTargetWord,
  TAccountPermission,
  TAccountPermissionProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOBLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: false },
    targetWord: { value: input.targetWord ?? null, isWritable: false },
    permission: { value: input.permission ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.targetWord),
      getAccountMeta(accounts.permission),
      getAccountMeta(accounts.permissionProgram),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateTargetWordPermissionInstructionDataEncoder().encode({}),
    programAddress,
  } as CreateTargetWordPermissionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountTargetWord,
    TAccountPermission,
    TAccountPermissionProgram,
    TAccountSystemProgram
  >);
}

export type ParsedCreateTargetWordPermissionInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    /** The player who owns the target word */
    player: TAccountMetas[1];
    /** The target word account to protect */
    targetWord: TAccountMetas[2];
    permission: TAccountMetas[3];
    permissionProgram: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: CreateTargetWordPermissionInstructionData;
};

export function parseCreateTargetWordPermissionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateTargetWordPermissionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      player: getNextAccount(),
      targetWord: getNextAccount(),
      permission: getNextAccount(),
      permissionProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateTargetWordPermissionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
