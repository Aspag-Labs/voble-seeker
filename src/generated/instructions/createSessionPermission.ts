/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VOBLE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CREATE_SESSION_PERMISSION_DISCRIMINATOR = new Uint8Array([
  3, 187, 129, 114, 173, 1, 177, 179,
]);

export function getCreateSessionPermissionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_SESSION_PERMISSION_DISCRIMINATOR,
  );
}

export type CreateSessionPermissionInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountSession extends string | AccountMeta<string> = string,
  TAccountPermission extends string | AccountMeta<string> = string,
  TAccountPermissionProgram extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountPlayer extends string
        ? ReadonlySignerAccount<TAccountPlayer> &
            AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountSession extends string
        ? ReadonlyAccount<TAccountSession>
        : TAccountSession,
      TAccountPermission extends string
        ? WritableAccount<TAccountPermission>
        : TAccountPermission,
      TAccountPermissionProgram extends string
        ? ReadonlyAccount<TAccountPermissionProgram>
        : TAccountPermissionProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateSessionPermissionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CreateSessionPermissionInstructionDataArgs = {};

export function getCreateSessionPermissionInstructionDataEncoder(): FixedSizeEncoder<CreateSessionPermissionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CREATE_SESSION_PERMISSION_DISCRIMINATOR,
    }),
  );
}

export function getCreateSessionPermissionInstructionDataDecoder(): FixedSizeDecoder<CreateSessionPermissionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCreateSessionPermissionInstructionDataCodec(): FixedSizeCodec<
  CreateSessionPermissionInstructionDataArgs,
  CreateSessionPermissionInstructionData
> {
  return combineCodec(
    getCreateSessionPermissionInstructionDataEncoder(),
    getCreateSessionPermissionInstructionDataDecoder(),
  );
}

export type CreateSessionPermissionAsyncInput<
  TAccountPayer extends string = string,
  TAccountPlayer extends string = string,
  TAccountSession extends string = string,
  TAccountPermission extends string = string,
  TAccountPermissionProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** The player who owns the session */
  player: TransactionSigner<TAccountPlayer>;
  /** The session account to protect */
  session?: Address<TAccountSession>;
  permission: Address<TAccountPermission>;
  permissionProgram: Address<TAccountPermissionProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getCreateSessionPermissionInstructionAsync<
  TAccountPayer extends string,
  TAccountPlayer extends string,
  TAccountSession extends string,
  TAccountPermission extends string,
  TAccountPermissionProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VOBLE_PROGRAM_ADDRESS,
>(
  input: CreateSessionPermissionAsyncInput<
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountPermission,
    TAccountPermissionProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateSessionPermissionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountPermission,
    TAccountPermissionProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOBLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: false },
    permission: { value: input.permission ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.session.value) {
    accounts.session.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([115, 101, 115, 115, 105, 111, 110]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.player.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.permission),
      getAccountMeta(accounts.permissionProgram),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateSessionPermissionInstructionDataEncoder().encode({}),
    programAddress,
  } as CreateSessionPermissionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountPermission,
    TAccountPermissionProgram,
    TAccountSystemProgram
  >);
}

export type CreateSessionPermissionInput<
  TAccountPayer extends string = string,
  TAccountPlayer extends string = string,
  TAccountSession extends string = string,
  TAccountPermission extends string = string,
  TAccountPermissionProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** The player who owns the session */
  player: TransactionSigner<TAccountPlayer>;
  /** The session account to protect */
  session: Address<TAccountSession>;
  permission: Address<TAccountPermission>;
  permissionProgram: Address<TAccountPermissionProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getCreateSessionPermissionInstruction<
  TAccountPayer extends string,
  TAccountPlayer extends string,
  TAccountSession extends string,
  TAccountPermission extends string,
  TAccountPermissionProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VOBLE_PROGRAM_ADDRESS,
>(
  input: CreateSessionPermissionInput<
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountPermission,
    TAccountPermissionProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateSessionPermissionInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountPlayer,
  TAccountSession,
  TAccountPermission,
  TAccountPermissionProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOBLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: false },
    permission: { value: input.permission ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.permission),
      getAccountMeta(accounts.permissionProgram),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateSessionPermissionInstructionDataEncoder().encode({}),
    programAddress,
  } as CreateSessionPermissionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountSession,
    TAccountPermission,
    TAccountPermissionProgram,
    TAccountSystemProgram
  >);
}

export type ParsedCreateSessionPermissionInstruction<
  TProgram extends string = typeof VOBLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    /** The player who owns the session */
    player: TAccountMetas[1];
    /** The session account to protect */
    session: TAccountMetas[2];
    permission: TAccountMetas[3];
    permissionProgram: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: CreateSessionPermissionInstructionData;
};

export function parseCreateSessionPermissionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateSessionPermissionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      player: getNextAccount(),
      session: getNextAccount(),
      permission: getNextAccount(),
      permissionProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateSessionPermissionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
